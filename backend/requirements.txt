# StudyMate backend — Python dependencies
# Install with: pip install -r requirements.txt

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
typing-extensions>=4.12.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0.0
# PostgreSQL driver, used when DATABASE_URL points at hosted Postgres (Render/Supabase).
psycopg2-binary>=2.9.9

# --- LLM & Orchestration ---
langchain-groq>=0.1.5
groq>=0.5.0
langgraph>=1.0.0
langgraph-checkpoint-sqlite>=3.0.0
reportlab>=4.0.0


# --- RAG (Module 2) ---
langchain>=1.3.0
langchain-community>=0.4.0
langchain-text-splitters>=0.2.0
faiss-cpu>=1.8.0
pypdf>=4.0.0
langsmith>=0.1.0
rank-bm25>=0.2.2
langchain-classic>=1.0.0

# --- Configuration & Utility ---
python-dotenv>=1.2.0
# HTTP client for the Cohere embed API (app/rag/embeddings.py).
# Declared explicitly: previously only available transitively.
requests>=2.31.0

# --- Authentication ---
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
email-validator==2.3.0

# --- Utility ---
ftfy==6.3.1